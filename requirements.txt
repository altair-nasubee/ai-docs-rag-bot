# 画面（フロント）
streamlit

# LangChain 本体と各種連携
langchain
langchain-community
langchain-groq          # 回答生成 LLM (Groq)
langchain-chroma        # ベクトルDB (Chroma) 連携
langchain-text-splitters

# ローカル埋め込み（ONNX・多言語）。onnxruntime は chromadb 依存で同梱される。
fastembed

# ベクトルDB
chromadb

# ドキュメント取得（.md を直接取得するため HTML パーサは不要）
requests

# 環境変数（ローカルの .env 読み込み）
python-dotenv
